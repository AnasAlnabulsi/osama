# Sicherheitsregeln

Nicht in Git speichern: echte `.env`, Passwörter, Nextcloud-Secrets, private TLS-Schlüssel,
Datenbank-Dumps, Nextcloud-Nutzdaten, VPN-/Router-Backups, Tokens oder API-Keys.

Die enthaltene Nextcloud `config.php` ist geschwärzt. Certbot-Account-IDs wurden entfernt.
